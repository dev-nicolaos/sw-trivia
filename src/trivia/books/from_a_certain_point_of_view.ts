import { TRIVIA, SOURCE } from '../../interfaces/mod.ts';

const source: SOURCE = {
  name: 'From a Certain Point of View',
  mediaType: 'Book',
};

const trivia: TRIVIA[] = [];

export default trivia;
