import { TRIVIA, SOURCE } from '../../interfaces/mod.ts';

const source: SOURCE = {
  name: 'Battlefront: Twighlight Company',
  mediaType: 'Book',
};

const trivia: TRIVIA[] = [];

export default trivia;
